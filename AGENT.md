# Compatibility Note

This repository now uses [`AGENTS.md`](./AGENTS.md) as the canonical agent-instruction file.

If your tooling still looks for `AGENT.md`, treat this file as a pointer and load `AGENTS.md` instead.
